<h2>Lista de Libros</h2>

<section>
    {% for book in books %}
    <article>
        <h3>{{ book.title }} <small>{{ book.author }} - {{ book.editionDate|date("m/d/Y") }}</small></h3>
        <img src="{{ asset('uploads/images/' ~ book.image ) }}">
        <p>{{ book.summary }}</p>
        <footer>
            <a href="{{ path('book_edit', { 'id': book.id }) }}" title="Edit">Editar</a>
        </footer>
    </article>
    {% endfor %}
</section>
